# This is workflow name 
name: Hello workflow
on:
  push:
    branches: (main)

# these are Job inside workflow
jobs:
# This is the name of Job
    greet:
      runs-on: ubuntu-latest
        steps:
        
            - name: Say hello to user
             run : echo "Hello Dosto"

            - name: sya hello to username
            run: echo "Hello $USER"
  
